
@model IEnumerable<WebApiCRUD.Models.Company>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Category List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Company" asp-action="Create" class="btn btn-primary">
                <i class="fas fa-plus"></i> &nbsp; Create New Company
            </a>
        </div>
    </div>

    <br /><br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Company Id
                    </th>
                    <th>
                     Company Name
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var obj in Model)
                {
                <tr>
                    <td width="50%">@obj.CompanyId</td>
                    <td width="30%">@obj.CompanyName</td>
                    <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Company" asp-route-Id="@obj.CompanyId" asp-action="Edit" class="btn btn-primary mx-2">
                                <i class="fas fa-edit"></i>  Edit
                            </a>
                            <a asp-controller="Company" asp-route-Id="@obj.CompanyId" asp-action="Delete" class="btn btn-danger mx-2">
                                <i class="far fa-trash-alt"></i> Delete
                            </a>
                        </div>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p> No Company exists.</p>
    }

</div>


